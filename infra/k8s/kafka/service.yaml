apiVersion: v1
kind: Service
metadata:
  name: kafka-headless
  namespace: notification-platform
spec:
  clusterIP: None
  publishNotReadyAddresses: true

  selector:
    app: kafka

  ports:
    - name: controller
      port: 9093
      targetPort: 9093