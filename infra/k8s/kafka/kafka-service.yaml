apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: notification-platform
spec:
  selector:
    app: kafka

  ports:
    - name: client
      port: 9092
      targetPort: 9092

  type: ClusterIP
